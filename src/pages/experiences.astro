---
import { getDb } from "@/db";
import {
  fetchExperienceCityCards,
  fetchExperiencesByCity,
} from "@/features/public/browse/experience-queries";
import type { Locale } from "@/i18n/types";
import { defaultLocale } from "@/i18n/types";

export const prerender = false;

// biome-ignore lint/correctness/noUnusedVariables: used in template
const locale = (Astro.currentLocale ?? defaultLocale) as Locale;
const db = getDb(Astro.locals.runtime.env.DB);
const cityParam = Astro.url.searchParams.get("city");
const currentPage = Math.max(1, Number(Astro.url.searchParams.get("page") || "1"));

// biome-ignore lint/correctness/noUnusedVariables: used in template
const cityCards = cityParam ? [] : await fetchExperienceCityCards(db);
// biome-ignore lint/correctness/noUnusedVariables: used in template
const { experiences, totalCount, totalPages } = cityParam
  ? await fetchExperiencesByCity(db, cityParam, currentPage)
  : { experiences: [], totalCount: 0, totalPages: 0 };
---

<PageLayout
  title={cityParam ? t(locale, "experiences.cityTitle", { city: cityParam }) : t(locale, "experiences.pageTitle")}
  description={t(locale, "experiences.description")}
  showMarble
>
  <Hero
    badge={t(locale, "experiences.badge")}
    title={t(locale, "experiences.title")}
    titleGradient={t(locale, "experiences.titleGradient")}
    subtitle={t(locale, "experiences.subtitle")}
    cityName={cityParam}
    height="medium"
  />

  {
    !cityParam ? (
      <section class="py-16 px-6">
        <div class="container mx-auto">
          <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8">
            {cityCards.map((card, index) => (
              <a
                href={`${localePath(locale, "/experiences")}?city=${encodeURIComponent(card.city)}`}
                class="group relative block rounded-2xl overflow-hidden aspect-4/3 card-hover"
                style={`animation-delay: ${0.1 + index * 0.1}s`}
              >
                <img
                  src={card.imageUrl}
                  alt={card.city}
                  class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-110"
                  loading="lazy"
                />
                <div class="absolute inset-0 bg-gradient-to-t from-black/60 via-black/40 to-black/20" />
                <div class="absolute inset-0 flex flex-col items-center justify-center text-center">
                  <h3 class="text-3xl md:text-4xl font-bold text-white">{card.city}</h3>
                  <p class="text-white/70 text-lg mt-1">
                    {card.count} {card.count === 1 ? t(locale, "common.experience") : t(locale, "common.experiences")}
                  </p>
                </div>
              </a>
            ))}
          </div>
        </div>
      </section>
    ) : (
      <section class="py-16 px-6">
        <div class="container mx-auto">
          <div class="mb-8 flex items-center gap-2 text-sm text-muted-foreground">
            <a
              href={localePath(locale, "/experiences")}
              class="hover:text-foreground transition-colors flex items-center gap-1"
            >
              <i data-lucide="arrow-left" class="w-4 h-4" />
              {t(locale, "common.allCities")}
            </a>
            <span>/</span>
            <span class="text-foreground">{cityParam}</span>
            <span class="ml-auto">
              {totalCount} {totalCount === 1 ? t(locale, "common.experience") : t(locale, "common.experiences")}
            </span>
          </div>

          <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            {experiences.map((exp, index) => (
              <a
                href={`${localePath(locale, "/experiences")}/${exp.id}`}
                target="_blank"
                class="group block rounded-2xl overflow-hidden bg-card card-hover animate-slide-up opacity-0"
                style={`animation-delay: ${0.1 + index * 0.1}s`}
              >
                <div class="relative aspect-[4/3] overflow-hidden">
                  <img
                    src={exp.imageUrl}
                    alt={exp.title}
                    class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-110"
                    loading="lazy"
                  />
                  <div class="absolute inset-0 bg-gradient-to-t from-black/60 via-transparent to-transparent" />
                  <div class="absolute top-4 left-4">
                    <span class="badge-elite">{exp.category}</span>
                  </div>
                  <div class="absolute bottom-4 right-4">
                    <span class="px-3 py-1 rounded-full bg-white/10 backdrop-blur-sm text-white text-sm">
                      {exp.duration}
                    </span>
                  </div>
                </div>
                <div class="p-5">
                  <h3 class="text-lg font-semibold text-foreground mb-1 group-hover:text-primary transition-colors">
                    {exp.title}
                  </h3>
                  <p class="text-muted-foreground text-sm flex items-center gap-1">
                    <i data-lucide="map-pin" class="w-4 h-4" />
                    {exp.location}
                  </p>
                </div>
              </a>
            ))}
          </div>

          {totalPages > 1 && (
            <nav class="mt-12 flex justify-center items-center gap-2">
              {currentPage > 1 && (
                <a
                  href={`${localePath(locale, "/experiences")}?city=${encodeURIComponent(cityParam)}&page=${currentPage - 1}`}
                  class="px-4 py-2 rounded-lg bg-card border border-border hover:bg-muted transition-colors text-sm"
                >
                  {t(locale, "common.previous")}
                </a>
              )}
              {Array.from({ length: totalPages }, (_, i) => i + 1).map(
                (page) => (
                  <a
                    href={`${localePath(locale, "/experiences")}?city=${encodeURIComponent(cityParam)}&page=${page}`}
                    class:list={[
                      "px-4 py-2 rounded-lg text-sm transition-colors",
                      page === currentPage
                        ? "bg-primary text-primary-foreground"
                        : "bg-card border border-border hover:bg-muted",
                    ]}
                  >
                    {page}
                  </a>
                )
              )}
              {currentPage < totalPages && (
                <a
                  href={`${localePath(locale, "/experiences")}?city=${encodeURIComponent(cityParam)}&page=${currentPage + 1}`}
                  class="px-4 py-2 rounded-lg bg-card border border-border hover:bg-muted transition-colors text-sm"
                >
                  {t(locale, "common.next")}
                </a>
              )}
            </nav>
          )}
        </div>
      </section>
    )
  }

  <section class="py-20 px-6 bg-card text-center">
    <div class="container mx-auto max-w-2xl">
      <Heading level={2} class="mb-4">
        {t(locale, "experiences.ctaHeading")}
      </Heading>
      <p class="text-muted-foreground mb-8">
        {t(locale, "experiences.ctaDescription")}
      </p>
      <Button href={localePath(locale, "/partner")}>{t(locale, "experiences.becomePartner")}</Button>
    </div>
  </section>
</PageLayout>
