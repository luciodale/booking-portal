/**
 * Experience API types. Re-export from schemas (single source of truth).
 */

export type {
  CreateExperienceInput,
  ExperienceListItem,
  ExperienceWithDetails,
  UpdateExperienceInput,
} from "@/schemas/experience";
