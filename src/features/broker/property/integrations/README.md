# Property integrations

One folder per external provider (e.g. import-from-pms). Each provider: `server-service/`, `ui/`, and provider-specific files.

Currently the "import from PMS listing" flow lives in property domain + pms routes.
